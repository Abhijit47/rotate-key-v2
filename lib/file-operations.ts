'use server';

export async function uploadFileToLocalServer(
  file: File,
  userId: string,
  locale: string
) {}

export async function deleteFileFromLocalServer(
  fileUrl: string,
  userId: string
) {}
